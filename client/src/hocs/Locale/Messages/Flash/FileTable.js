import { defineMessages } from 'react-intl';

export const scope = 'coiliiot.app.flash.fileTable';

export default defineMessages({
  addressHeader: {
    id: `${scope}.addressHeader`,
    defaultMessage: 'Address',
  },
  fileHeader: {
    id: `${scope}.fileHeader`,
    defaultMessage: 'File',
  },
  statusHeader: {
    id: `${scope}.statusHeader`,
    defaultMessage: 'Status',
  },
  actionHeader: {
    id: `${scope}.actionHeader`,
    defaultMessage: 'Actions',
  },
});
