import { defineMessages } from 'react-intl';

export const scope = 'coiliiot.app.common.errorModal';

export default defineMessages({
  typography: {
    id: `${scope}.typography`,
    defaultMessage: 'Error Warning',
  },
});
