import { defineMessages } from 'react-intl';

export const scope = 'coiliiot.app.common.noRowsOverlay';

export default defineMessages({
  message: {
    id: `${scope}.message`,
    defaultMessage: 'No Rows',
  },
});
